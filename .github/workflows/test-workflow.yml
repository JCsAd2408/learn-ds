name: Test workflow

on:
  push:
  pull_request:

jobs:
  test-job:
    name: Test Job
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/test
        with:
          message: "Experimenting with custom actions"

